# Role Realization Shape

A role (BFO_0000023) must be realized in at least one process via `bfo:realized_in`.

```ontoink
source: shapes/role-realization/shape-data.ttl
shape: shapes/role-realization/shape.ttl
```
